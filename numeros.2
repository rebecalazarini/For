//programa que escreva os numerais de 20 a 1 em for
#include <stdio.h>
int main(){
   int numeros;
  for(numeros = 20; numeros >= 1; numeros--)
  {
    printf("\n %d", numeros);
  }
  
  return(0);
}
